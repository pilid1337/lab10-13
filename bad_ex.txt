program ErrorProgram;
var
    alpha, beta : integer;
    gamma : real;
    arr : array [1..10] of integer;
begin
    { Ошибка 1: Использование необъявленной переменной }
    omega := 10; 

    { Ошибка 2: Несовместимые типы данных при присваивании (real в integer нельзя) }
    alpha := gamma; 

    { Ошибка 3: Условие в IF должно иметь логический тип (а тут integer) }
    if alpha then 
    begin
        beta := 1;
    end;

    { Ошибка 4: Попытка применить индекс к переменной, не являющейся массивом }
    beta[2] := 10; 

    { Ошибка 5: Индекс массива должен быть только целого типа (а тут real) }
    arr[3.14] := 5; 
end.